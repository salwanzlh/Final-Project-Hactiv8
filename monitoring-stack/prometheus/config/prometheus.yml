global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: showroom_ai
    static_configs:
      - targets: ['host.docker.internal:8000']
    metrics_path: /metrics
